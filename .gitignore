# ----------------------------------------------------------------------
# 1. Dependency and Build Files
# ----------------------------------------------------------------------
# Node dependencies folder
node_modules/
# Log files generated by npm/yarn
npm-debug.log*
yarn-debug.log*

# ----------------------------------------------------------------------
# 2. Environment Variables & Credentials (CRITICAL for security)
# ----------------------------------------------------------------------
# NEVER commit your secrets, including the database connection string and JWT key
.env
.env.local
.env.*

# ----------------------------------------------------------------------
# 3. Prisma Files
# ----------------------------------------------------------------------
# The Prisma Client is generated locally; don't commit it.
# It is rebuilt automatically from the schema during deployment.
/node_modules/.prisma/client/
# Local database files (if you are using a file-based database like SQLite for dev)
*.sqlite
*.sqlite-journal

# ----------------------------------------------------------------------
# 4. OS and Editor Files
# ----------------------------------------------------------------------
# macOS
.DS_Store
# Windows
Thumbs.db
# VSCode or other common editor files
.vscode/
*.sublime-project